(ns chip8.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  ;;(setup-graphics)
  ;;(setup-input)

  ;;(init-cpu)
  ;;(load-game game)
  (while true
   ;; (cpu-emulate-cycle)
    ;;(if (cpu :draw-flag) (draw-graphics))
    ;;(set-keys)
    )
  )
